<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="MypageMapper">

	<select id="assetsList"
			resultType="kr.co.creator.vo.InvestVO">
		select invest_num
			, project_num
			, invest_price
			, invest_date
			FROM invest_list
			order by invest_date desc
	</select>
	
	<select id="InvestList"
			resultType="kr.co.creator.vo.InvestVO">
		select  i.invest_date
			, i.project_num
			, p.project_name
			, p.rate
			, i.invest_price
			, p.refund
		from invest_list i
		left outer join project_list p
        on i.project_num = p.project_num
		where i.project_num = #{project_num}
	</select>
	
</mapper>